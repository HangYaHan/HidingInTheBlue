# Contributing Guide

FPGA 比赛团队协作仓库。GitHub 新手请先读本文件。

## 规则

1. **不要直接改 `main`**。所有开发在分支上进行。
2. **不要上传中间文件和大文件**：编译输出、日志、缓存、测试视频等。
3. **临时文件分开放**：
   - 本地私有文件 → `local/`
   - 不确定归属 / 中间产物 → `temp/`
4. **一次提交只做一件事**。不要把无关改动混在一起。
5. **改顶层目录、约束、接口前先说一声**。

## 流程

1. 同步最新 `main`
2. 从 `main` 创建分支：`feature/<名>`、`fix/<名>`、`docs/<名>`
3. 在分支上开发
4. 发起 Pull Request
5. 合并后删除分支

提交信息格式：`type: 简短描述`
